using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class rff : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // LoadBannerAd()
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
